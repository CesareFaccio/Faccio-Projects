# Cesare's Projects Site

A static site (Vite + TypeScript) listing personal projects, each with its
own interactive page. No backend — everything runs client-side.

## Development

    npm install
    npm run dev

## Build

    npm run build   # outputs to dist/
